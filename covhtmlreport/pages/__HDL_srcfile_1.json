var g_sourceLines = [
'`ifndef ALU_IF_VH',
'`define ALU_IF_VH',
'',
'// all types',
'`include "cpu_types_pkg.vh"',
'',
'interface alu_if;',
'  // import types',
'  import cpu_types_pkg::*;',
'',
'  logic     negative, zero, overflow;',
'  aluop_t   aluop; // 4 bit opcode to select operation to perform',
'  word_t    portA, portB, outputPort;',
'',
'  // ALU ports',
'  modport alu (',
'    input   portA, portB, aluop,',
'    output  outputPort, negative, zero, overflow',
'  );',
'  // ALU tb',
'  modport tb (',
'    input   outputPort, negative, zero, overflow,',
'    output  portA, portB, aluop',
'  );',
'endinterface',
'',
'`endif //ALU_IF_VH',
''
];
countinueBuildStmtTable(g_sourceLines);
